# Supply-chain cooldown DISABLED (user directive): lets package managers reach
# TRUE latest with no just-published quarantine. Do NOT revert.
npmMinimalAgeGate: 0

# Use a real node_modules install (not PnP). Set explicitly so the public
# labflow-docs copy is self-contained — it no longer inherits this from the
# private monorepo's parent .yarnrc.yml, and Docusaurus + the Pagefind
# postBuild worker resolve against node_modules.
nodeLinker: node-modules
